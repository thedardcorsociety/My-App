<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="theme-color" content="#0b0c15">
    <title>Dardcor AI</title>
    
    <link rel="icon" href="/logo.png">
    <link rel="manifest" href="/manifest.json">

    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/atom-one-dark.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/mermaid@10.9.1/dist/mermaid.min.js"></script>

    <link rel="stylesheet" href="/css/tailwind.css">
    <link rel="stylesheet" href="/vendor/fontawesome/css/all.min.css">
    <link rel="stylesheet" href="/css/atom-one-dark.min.css">
    <script src="/js/highlight.min.js"></script>
    <script src="/js/marked.min.js"></script>

    <style>
        html, body { height: 100dvh; width: 100%; margin: 0; padding: 0; overflow: hidden; background-color: #0b0c15; position: fixed; inset: 0; color: white; font-family: sans-serif; }
        .scrollbar-hide::-webkit-scrollbar { display: none; }
        .scrollbar-hide { -ms-overflow-style: none; scrollbar-width: none; }
        pre { margin: 0; padding: 0; background: transparent; }
        .safe-pt { padding-top: env(safe-area-inset-top); }
        .safe-pb { padding-bottom: env(safe-area-inset-bottom); }
        .safe-pl { padding-left: env(safe-area-inset-left); }
        .message-bubble { max-width: 95%; }
        .terminal-container { margin: 12px 0; background-color: #000000; border: 1px solid #333; border-radius: 6px; overflow: hidden; font-family: 'Consolas', monospace; width: 100%; box-shadow: 0 4px 6px -1px rgba(0,0,0,0.1); }
        .terminal-head { display: flex; justify-content: space-between; align-items: center; background-color: #111; padding: 6px 12px; border-bottom: 1px solid #333; height: 40px; }
        .terminal-label { color: #ccc; font-size: 11px; font-weight: 700; text-transform: uppercase; display: flex; align-items: center; gap: 6px; }
        .terminal-opts { display: flex; gap: 8px; align-items: center; }
        .cmd-btn { display: flex; align-items: center; gap: 5px; padding: 4px 10px; border-radius: 4px; font-size: 11px; font-weight: 600; cursor: pointer; border: none; transition: all 0.2s; }
        .btn-preview { background-color: #0e639c; color: white; }
        .btn-preview:hover { background-color: #1177bb; }
        .btn-diagram { background-color: #7c3aed; color: white; }
        .btn-diagram:hover { background-color: #8b5cf6; }
        .btn-cp { background-color: #333; color: #e5e5e5; }
        .btn-cp:hover { background-color: #444; }
        .terminal-code { padding: 0; overflow-x: auto; background-color: #000000; color: #f8f8f2; }
        .terminal-code pre { margin: 0; }
        .terminal-code code { font-family: 'Consolas', monospace; font-size: 13px; line-height: 1.5; background: transparent !important; padding: 16px !important; display: block; }
        .markdown-body { line-height: 1.6; font-size: 0.95rem; }
        .markdown-body p { margin-bottom: 1em; }
        .markdown-body h1, .markdown-body h2, .markdown-body h3 { margin-top: 1.5em; margin-bottom: 0.5em; font-weight: bold; color: #e2e8f0; }
        .markdown-body ul { list-style-type: disc; margin-bottom: 1em; padding-left: 1.5em; }
        .markdown-body ol { list-style-type: decimal; margin-bottom: 1em; padding-left: 1.5em; }
        .markdown-body a { color: #a78bfa; text-decoration: underline; }
        .markdown-body blockquote { border-left: 4px solid #4b5563; padding-left: 1em; color: #9ca3af; font-style: italic; }
        .loader { border: 3px solid rgba(139, 92, 246, 0.2); border-top: 3px solid #8b5cf6; border-radius: 50%; width: 20px; height: 20px; animation: spin 1s linear infinite; }
        @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
    </style>
</head>
<body class="flex text-white font-sans">
    
    <script>
        (function() {
            if (window.innerWidth >= 1024) {
                const savedState = localStorage.getItem('dardcor_sidebar_state');
                if (savedState === 'closed') {
                    document.body.classList.add('sidebar-closed');
                }
            }
        })();
    </script>
    
    <style>
        @media (min-width: 1024px) {
            body.sidebar-closed #sidebar { transform: translateX(-100%); }
            body.sidebar-closed #main-content { left: 0; }
            body:not(.sidebar-closed) #main-content { left: 18rem; }
            body:not(.sidebar-closed) #sidebar { transform: translateX(0); }
        }
    </style>

    <div id="mobile-overlay" class="fixed inset-0 bg-black/60 z-40 hidden backdrop-blur-sm transition-opacity lg:hidden"></div>

    <aside id="sidebar" class="fixed inset-y-0 left-0 w-72 bg-[#13131f] border-r border-purple-900/30 flex flex-col transform -translate-x-full lg:translate-x-0 z-[60] shadow-2xl safe-pl transition-transform duration-300">
        <div class="h-16 shrink-0 flex items-center justify-between px-4 border-b border-purple-900/30 safe-pt">
            <span class="font-bold text-lg tracking-wider text-purple-500 flex items-center gap-2">
                <img src="/logo.png" class="w-6 h-6 rounded-full"> Dardcor <span class="text-white">AI</span>
            </span>
            <button id="close-sidebar-btn" class="text-gray-400 hover:text-white p-2 rounded hover:bg-white/10 transition-colors">
                <i class="fas fa-times text-lg"></i>
            </button>
        </div>
        
        <div class="flex-1 overflow-y-auto p-3 space-y-1.5 scrollbar-hide relative z-40">
            <button id="new-chat-btn" class="group relative flex items-center pl-[15px] pr-3 h-[44px] rounded-lg border border-gray-600/30 hover:bg-[#202336] text-left transition-all w-full mb-4">
                <div class="flex-1 min-w-0 flex items-center h-full pointer-events-none">
                    <div class="w-5 flex justify-center text-gray-500 group-hover:text-white mr-3 flex-shrink-0 transition-colors">
                        <i class="fas fa-plus text-xs"></i>
                    </div>
                    <span class="text-sm text-gray-300 group-hover:text-white font-medium truncate transition-colors">New Chat</span>
                </div>
            </button>
            <div class="text-gray-500 text-center text-xs font-semibold uppercase tracking-wider mb-2 px-2">Riwayat</div>
            
            <% 
                let isNewChat = true; 
                if (conversationList && conversationList.length > 0) { 
                    isNewChat = !conversationList.some(c => c.id === activeConversationId); 
                } 
            %>
            
            <div id="current-new-chat-item" class="group relative flex items-center px-3 h-[44px] rounded-lg cursor-pointer w-full mb-1 <%= isNewChat ? 'bg-[#202336] border-l-4 border-purple-500' : 'border-l-4 border-transparent hover:bg-[#202336]' %>">
                <div class="flex-1 min-w-0 pr-2 flex items-center h-full pointer-events-none">
                    <div class="w-5 flex justify-center text-gray-500 mr-3 flex-shrink-0">
                        <i class="fas fa-message text-xs"></i>
                    </div>
                    <span class="text-sm text-white truncate">Percakapan Baru</span>
                </div>
            </div>

            <% if (conversationList) { conversationList.forEach(chat => { %>
                <div id="chat-item-<%= chat.id %>" onclick="loadChat('<%= chat.id %>')" class="group relative flex items-center px-3 h-[44px] rounded-lg hover:bg-[#202336] cursor-pointer w-full border-l-4 <%= chat.id === activeConversationId ? 'bg-[#202336] border-purple-500' : 'border-transparent' %>">
                    <div class="flex-1 min-w-0 pr-2 flex items-center h-full pointer-events-none">
                        <div class="w-5 flex justify-center text-gray-500 mr-3 flex-shrink-0">
                            <i class="fas fa-message text-xs"></i>
                        </div>
                        <span id="title-<%= chat.id %>" class="text-sm text-gray-300 truncate"><%= chat.title ? chat.title.substring(0, 22) : 'Percakapan' %>...</span>
                        <input type="hidden" id="raw-title-<%= chat.id %>" value="<%= chat.title %>">
                    </div>
                    <div class="relative flex items-center h-full">
                        <button onclick="event.stopPropagation(); toggleMenu(event, 'menu-<%= chat.id %>')" class="p-1.5 text-gray-500 hover:text-white rounded opacity-0 group-hover:opacity-100 focus:opacity-100"><i class="fas fa-ellipsis-v text-xs"></i></button>
                        <div id="menu-<%= chat.id %>" class="dropdown-menu hidden absolute right-0 top-8 w-32 bg-[#2a2d3d] border border-gray-700 rounded-lg shadow-xl z-[70] py-1 overflow-hidden">
                            <button onclick="openRenameModal('<%= chat.id %>')" class="w-full text-left px-3 py-2 text-xs text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-2"><i class="fas fa-edit"></i> Edit</button>
                            <button onclick="openDeleteModal('<%= chat.id %>')" class="w-full text-left px-3 py-2 text-xs text-red-400 hover:bg-red-600 hover:text-white flex items-center gap-2"><i class="fas fa-trash"></i> Delete</button>
                        </div>
                    </div>
                </div>
            <% }) } %>
        </div>
        <div class="p-3 border-t border-purple-900/30 bg-[#0e0f19] min-h-[4rem] flex items-center shrink-0 safe-pb">
            <a href="/dardcorchat/profile" class="flex items-center text-gray-400 hover:text-white transition w-full gap-3">
                <img src="<%= user.profile_image || 'https://ui-avatars.com/api/?name=' + user.username %>" class="w-8 h-8 rounded-full border border-gray-700 object-cover">
                <div class="flex flex-col overflow-hidden"><span class="text-sm font-medium text-gray-200 truncate"><%= user.username %></span><span class="text-[10px] text-gray-500">Profile</span></div>
            </a>
            <a href="/dardcor-logout" class="ml-2 text-gray-500 hover:text-red-400 p-2"><i class="fas fa-sign-out-alt"></i></a>
        </div>
    </aside>

    <main id="main-content" class="absolute inset-0 right-0 transition-all duration-300 flex flex-col bg-[#0b0c15] lg:left-72">
        <header class="h-16 shrink-0 flex items-center justify-between px-4 border-b border-purple-900/30 bg-[#0b0c15]/95 backdrop-blur-md z-50 safe-pt">
            <div class="flex items-center">
                <button id="sidebar-toggle-btn" class="text-gray-400 p-2 rounded hover:bg-gray-800 transition-colors">
                    <i class="fas fa-bars text-lg"></i>
                </button>
            </div>
            <div class="flex items-center gap-2 ml-auto">
                <button id="refresh-chat-btn" class="flex items-center justify-center w-[34px] h-[34px] rounded-lg border border-purple-500/30 bg-purple-500/10 text-purple-400 hover:bg-purple-500/20 hover:text-white transition-all">
                    <i id="refresh-icon" class="fas fa-redo-alt text-xs"></i>
                </button>
                <div class="relative">
                    <button id="model-dropdown-btn" class="flex items-center gap-2 px-3 py-1.5 rounded-lg border border-purple-500/30 bg-purple-500/10 text-purple-400 text-xs md:text-sm font-medium hover:bg-purple-500/20 hover:text-white transition-all h-[34px]">
                        <span id="tool-label"><%= toolType === 'dark' ? 'Dark Model' : 'Basic Model' %></span> <i class="fas fa-chevron-down text-[10px] ml-1 transition-transform" id="tools-chevron"></i>
                    </button>
                    <div id="tools-menu" class="hidden absolute right-0 top-full mt-2 w-fit bg-[#0b0c15] border border-purple-500/30 rounded-lg shadow-xl shadow-purple-900/20 z-[100] overflow-hidden">
                        <button data-model="basic" class="model-select-btn w-full text-left px-3 py-2 text-xs md:text-sm font-medium text-purple-400 hover:bg-purple-500/20 hover:text-white transition-all border-b border-purple-500/30 whitespace-nowrap">Basic Model</button>
                        <button data-model="dark" class="model-select-btn w-full text-left px-3 py-2 text-xs md:text-sm font-medium text-purple-400 hover:bg-purple-500/20 hover:text-white transition-all whitespace-nowrap">Dark Model</button>
                    </div>
                </div>
            </div>
        </header>

        <div id="chat-container" class="flex-1 overflow-y-auto scrollbar-hide px-4 pt-4 w-full relative">
            <%- include(contentPage) %>
            <div class="h-[120px]"></div> 
        </div>

        <%- include('input') %>
    </main>

    <div id="delete-modal" class="fixed inset-0 z-[110] hidden items-center justify-center bg-black/80 backdrop-blur-sm p-4 safe-pb">
        <div class="bg-[#13131f] border border-purple-900 rounded-2xl w-full max-w-sm p-6 text-center">
            <h3 class="text-lg font-bold text-white mb-4">Hapus Chat?</h3>
            <div class="flex justify-center gap-3">
                <button onclick="closeModal('delete-modal')" class="px-5 py-2.5 text-gray-300 bg-gray-800 rounded-lg w-full">Batal</button>
                <button onclick="submitDelete()" class="px-5 py-2.5 bg-red-600 text-white rounded-lg w-full">Hapus</button>
            </div>
        </div>
    </div>

    <div id="rename-modal" class="fixed inset-0 z-[110] hidden items-center justify-center bg-black/80 backdrop-blur-sm p-4 safe-pb">
        <div class="bg-[#13131f] border border-purple-900 rounded-2xl w-full max-w-sm p-6 text-center">
            <h3 class="text-lg font-bold text-white mb-4">Ubah Nama</h3>
            <input type="text" id="rename-input" class="w-full bg-black/30 border border-gray-700 rounded-lg p-3 text-white mb-4 outline-none focus:border-purple-500" placeholder="Nama chat baru...">
            <div class="flex justify-center gap-3">
                <button onclick="closeModal('rename-modal')" class="px-5 py-2.5 text-gray-300 bg-gray-800 rounded-lg w-full">Batal</button>
                <button onclick="submitRename()" class="px-5 py-2.5 bg-purple-600 text-white rounded-lg w-full">Simpan</button>
            </div>
        </div>
    </div>

    <script>
        window.SERVER_DATA = {
            currentConversationId: "<%= activeConversationId || '' %>",
            toolType: "<%= typeof toolType !== 'undefined' ? toolType : 'basic' %>"
        };
    </script>
    <script src="/js/dardcorai.js"></script>
</body>
</html>