<div id="message-list" class="max-w-3xl mx-auto space-y-6 pb-2 px-2 md:px-0">
    <% if (!chatHistory || chatHistory.length === 0) { %>
        <div id="empty-state" class="flex flex-col items-center justify-center text-gray-500 opacity-60 min-h-[50vh]">
            <div class="w-24 h-24 bg-gray-800/50 rounded-full flex items-center justify-center mb-6 overflow-hidden border border-gray-700 shadow-xl bg-black">
                <% if (typeof toolType !== 'undefined' && toolType === 'image') { %>
                    <i class="fas fa-image text-4xl text-purple-500"></i>
                <% } else { %>
                    <img src="/logo.png" alt="Dardcor Logo" class="w-full h-full object-cover">
                <% } %>
            </div>
            <p class="text-lg font-medium text-gray-400 text-center">
                <% if (typeof toolType !== 'undefined' && toolType === 'image') { %>
                    Mulai buat gambar AI.
                <% } else { %>
                    Apa yang bisa saya bantu?
                <% } %>
            </p>
        </div>
    <% } else { %>
        <% chatHistory.forEach(msg => { %>
            <div class="flex w-full <%= msg.role === 'user' ? 'justify-end' : 'justify-start' %> message-bubble-container">
                <div class="flex flex-col <%= msg.role === 'user' ? 'items-end' : 'items-start' %> message-bubble w-full max-w-[95%]">
                    <div class="flex items-center gap-2 mb-1 px-1">
                        <span class="text-[10px] uppercase tracking-wide text-gray-500 font-bold"><%= msg.role === 'user' ? 'You' : 'Dardcor AI' %></span>
                    </div>
                    <div class="rounded-2xl p-4 shadow-lg text-sm leading-relaxed bg-[#13131f] text-gray-200 rounded-bl-none border border-gray-800 w-full overflow-hidden <%= msg.role === 'user' ? '!bg-violet-600 !text-white !rounded-br-none !rounded-bl-2xl !border-none' : '' %>">
                        <% if (msg.file_metadata) { %>
                            <div class="mb-3 text-xs flex items-center gap-3 bg-black/20 p-2 rounded-lg border border-white/10">
                                <i class="fas fa-file text-base"></i>
                                <div class="flex flex-col overflow-hidden min-w-0">
                                    <span class="truncate font-medium text-white text-xs-mobile"><%= msg.file_metadata.filename %></span>
                                    <span class="opacity-70 text-[10px]"><%= Math.round(msg.file_metadata.size / 1024) %> KB</span>
                                </div>
                            </div>
                        <% } %>
                        
                        <textarea class="hidden raw-message-content"><%= msg.message %></textarea>
                        <div class="markdown-body"></div>
                        
                    </div>
                </div>
            </div>
        <% }) %>
    <% } %>
    
    <div id="loading-indicator" class="hidden flex w-full justify-start">
        <div class="flex flex-col items-start message-bubble">
            <span class="text-[10px] uppercase tracking-wide text-gray-500 font-bold mb-1 px-1">Dardcor AI</span>
            <div class="bg-[#13131f] px-4 py-3 rounded-2xl rounded-bl-none border border-gray-800 flex items-center gap-2 shadow-md">
                <div class="flex space-x-1.5">
                    <div class="w-2 h-2 bg-purple-500 rounded-full animate-bounce"></div>
                    <div class="w-2 h-2 bg-purple-500 rounded-full animate-bounce" style="animation-delay: 0.15s"></div>
                    <div class="w-2 h-2 bg-purple-500 rounded-full animate-bounce" style="animation-delay: 0.3s"></div>
                </div>
                <span id="loading-text" class="text-xs text-gray-500 ml-2">Sedang memproses...</span>
            </div>
        </div>
    </div>
</div>